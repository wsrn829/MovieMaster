import ReactDOM from 'react-dom/client';

import App from './App';


ReactDOM.createRoot(document.getElementById('root')).render(<App />);



//The way React works is all React components are injected and rendered inside the root div here.